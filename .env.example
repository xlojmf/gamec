# Keep the browser build and Docker host game port in sync.
CATAN_WEB_PORT=3000
CATAN_GAME_PORT=8000
VITE_GAME_SERVER_PORT=8000
# Public HTTPS origin for Coolify (baked into the client at build time).
# Leave empty for local development.
VITE_GAME_SERVER_URL=
